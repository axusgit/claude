# Install with:
#   pip install soundcard sounddevice numpy pillow pystray faster-whisper
#   pip install torch==2.9.1 torchaudio==2.9.1 --index-url https://download.pytorch.org/whl/cpu
#   pip install pyannote.audio
# (ffmpeg must be on PATH — it already is at C:\ffmpeg\bin)
soundcard
sounddevice
numpy
pillow
pystray
faster-whisper
torch==2.9.1
torchaudio==2.9.1
pyannote.audio
