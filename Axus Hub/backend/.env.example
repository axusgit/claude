DATABASE_URL=postgresql://axushub:AxusHub2026!@localhost/axushub
SECRET_KEY=change-this-to-a-random-64-character-string
ACCESS_TOKEN_EXPIRE_MINUTES=480

# Server binding (see run.py). HOST=0.0.0.0 makes it reachable externally.
# Set PORT to a port that is open in your AWS security group.
HOST=0.0.0.0
PORT=8000

# For local development without PostgreSQL, use SQLite instead:
# DATABASE_URL=sqlite:///./axushub.db
