{
  "_comment": "Fast-alert sniper config. Scans all 6 cabins for any bookable 2-night stay. intervalMs = --arm burst sleep between polls (100ms per Andy) across the 8PM ET release window (windowStartET/EndET, 24h ET). huntSec = --hunt/--tick cadence. ntfyTopic falls back to ../.env NTFY_TOPIC; email/SMS use ../.env SMTP_*/EMAIL_*.",
  "intervalMs": 100,
  "huntSec": 30,
  "windowStartET": "19:59:55",
  "windowEndET": "20:00:55"
}
